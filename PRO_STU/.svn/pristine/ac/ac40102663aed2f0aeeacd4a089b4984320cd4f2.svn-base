.blocker { padding: 5px; font-size: 12px; position: absolute; z-index: 100000; }
  .blocker-inner { padding: 5px 8px 4px 8px; background-color: black; color: white; max-width: 200px; text-align: center; }
  .blocker-inner { border-radius: 3px; -moz-border-radius:3px; -webkit-border-radius:3px; }
  .blocker-arrow { position: absolute; background: url() no-repeat top left; width: 9px; height: 5px; }
  .blocker-n .blocker-arrow { top: 0; left: 50%; margin-left: -4px; }
    .blocker-nw .blocker-arrow { top: 0; left: 10px; }
    .blocker-ne .blocker-arrow { top: 0; right: 10px; }
  .blocker-s .blocker-arrow { bottom: 0; left: 50%; margin-left: -4px; background-position: bottom left; }
    .blocker-sw .blocker-arrow { bottom: 0; left: 10px; background-position: bottom left; }
    .blocker-se .blocker-arrow { bottom: 0; right: 10px; background-position: bottom left; }
  .blocker-e .blocker-arrow { top: 50%; margin-top: -4px; right: 0; width: 5px; height: 9px; background-position: top right; }
  .blocker-w .blocker-arrow { top: 50%; margin-top: -4px; left: 0; width: 5px; height: 9px; }